# Agent Connect Protocol Setup

Using `uv` to develop this:

```bash
uv init
uv venv
uv add agntcy-acp
```

Documentation:

- General UV Tool Guide: https://docs.agntcy.org/
- Agent Connect Protocol (ACP): https://docs.agntcy.org/syntactic/connect/